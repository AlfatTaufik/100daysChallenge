// Conditional 
function sayHello(nama:string) {
    switch(nama) {
        case "Rubben":
            return "Hello Rubben"
        default: 
            return `Hello ${nama}`
    }
}
if (10 > 2) console.log("10 lebih besar")
80 >= 75 ? "Lulus" : "Coba lagi"